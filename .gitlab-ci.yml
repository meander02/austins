stages:
  - build
  - test

build:
  stage: build
  script:
    - echo "Construyendo el proyecto..."

test:
  stage: test
  script:
    - echo "Ejecutando pruebas unitarias..."
    # Agrega comandos para instalar dependencias y ejecutar pruebas aquí
    - npm install -g @angular/cli # Instala Angular CLI globalmente (si no está instalado)
    - npm install # Instala las dependencias del proyecto
    - ng test --include="**/payment.module.spec.ts" # Ejecuta las pruebas para el módulo PaymentModule
